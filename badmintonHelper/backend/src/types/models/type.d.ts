export interface GoodFieldType {
    id: number
    goodname: string
    price: number
    desc?: string
}

export interface UserFieldType {
    id: number
    username: string
    password: string
    desc?: string
}