Component({
  /**
   * 组件的属性列表
   */
  properties: {
    // avatarList: [
    //   {path: ""}
    // ],
    // b: 222
  },

  /**
   * 组件的初始数据
   */
  data: {
    // curAvatarList: [
    //   {path: ""}
    // ],
    aa: 111
  },

  /**
   * 组件的方法列表
   */
  methods: {
    // 监听avatarList属性的变化，修改curAvatarList的值：
    // changeAvatarList() {
    //   if(this.properties.avatarList.length > 10) {
    //     this.setData({
    //       curAvatarList: this.properties.avatarList.slice(0, 10)
    //     })
    //   } else {
    //     this.setData({
    //       curAvatarList: this.properties.avatarList
    //     })
    //   }
   
    // }
  }
})